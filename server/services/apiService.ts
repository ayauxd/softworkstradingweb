import { storage } from '../storage';

/**
 * Service layer for API business logic
 */
export const apiService = {
  /**
   * Example service method for future implementation
   */
  exampleMethod: async () => {
    // This is where business logic would go
    // It would interact with storage or external APIs
    return { result: 'example data' };
  }
};